name: Release

on:
  workflow_dispatch:
  pull_request:
    branches:
      - dev

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Restore signing keystore
        run: |
          echo "${{ secrets.SIGNING_KEYSTORE_B64 }}" | base64 -d > key.keystore

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Gradle cache
        uses: burrunan/gradle-cache-action@v1

      - name: Build & Sign Release APK
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          chmod +x ./gradlew
          ./gradlew assembleRelease \
            -Psigning.storeFile=${{ github.workspace }}/key.keystore \
            -Psigning.storePassword="${{ secrets.SIGNING_STORE_PASSWORD }}" \
            -Psigning.keyAlias="${{ secrets.SIGNING_KEY_ALIAS }}" \
            -Psigning.keyPassword="${{ secrets.SIGNING_KEY_PASSWORD }}" \
            --no-daemon --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Mystery-Manager
          path: |
            app/build/outputs/apk/release/*.apk
